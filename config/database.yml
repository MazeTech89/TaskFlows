default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

  # Personalizable values:
  username: <%= ENV.fetch("PG_USERNAME") { "mosesa" } %>
  password: <%= ENV.fetch("PG_PASSWORD") { "DanZiah132201$$" } %>
  # Prefer the IPv4 loopback for database connections to avoid IPv6/::1 auth issues on some Windows setups.
  host: <%= ENV.fetch("PG_HOST") { "127.0.0.1" } %>
  port: <%= ENV.fetch("PG_PORT") { 5432 } %>

development:
  <<: *default
  username: <%= ENV.fetch("PG_USERNAME") { "mosesa" } %>
  password: <%= ENV.fetch("PG_PASSWORD") { "DanZiah132201$$" } %>
  database: taskflows_development

test:
  <<: *default
  database: taskflows_test

production:
  primary:
    <<: *default
    database: taskflows_production

  cache:
    <<: *default
    database: taskflows_cache_production

  queue:
    <<: *default
    database: taskflows_queue_production

  cable:
    <<: *default
    database: taskflows_cable_production
